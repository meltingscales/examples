# Shapes

ASCII art and graphical shape visualization with Mandelbrot sets.

## Setup

```bash
uv venv
source .venv/bin/activate
uv pip install -e .
```

## Usage

```bash
python shapes.py          # ASCII shapes and Mandelbrot
python shapes_graphical.py # Interactive Mandelbrot viewer
```